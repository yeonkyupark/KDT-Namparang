# 남파랑길 가이드 웹 서비스 — 구축 계획

> 부산 오륙도 ~ 해남 땅끝탑, 90개 코스(+임시노선 2개)의 GPX 기반 개인용 가이드 웹앱.
> **서버 0개** — GitHub Pages(호스팅) + GitHub 리포(사진·메모 저장).

**확정 사항**
- GPX 출처: [Daum 카페 "도보여행(섬&산) 좋은사람들"](https://cafe.daum.net/mtsingles/LN1X/1631) — 92개 확보·검증 완료
- 참고 자료: [gpsroute.site](https://gpsroute.site/courses/nampparang) — 코스별 누적상승 90개. **검증 결과 과대계상으로 확인돼 기준에서 배제**(3.1.1)
- 사진/메모 저장: **GitHub Contents API** (Supabase 폐기)
- 리포: Public + GitHub Pages

---

## 0. 데이터 조사 결과

### 0.1 Daum 카페 GPX — 채택 (2026-08-27 전수 검증 완료)

92개 GPX를 모두 다운로드해 검증했다. 원시 결과는 `data/raw/validate.json`.

| 항목 | 결과 | 판단 |
|---|---|---|
| 파일 수 | 92개 = 본 코스 90 + `17-1`(거제 임시우회) + `61-1`(순천만 흑두루미 우회) | ✅ 완전 |
| 총 거리 | **1,476.0 km** (하버사인, 원본 해상도) | ✅ 공식 약 1,470km와 일치 |
| 코스별 거리 | 3.44 ~ 27.51 km | ✅ 정상 |
| 총 트랙포인트 | **134,697개** (코스당 55~4,211) | ✅ 단순화하면 충분히 가벼움 |
| **코스 연결성** | 90개 연속 코스의 접합부 89곳 **모두 500m 이내** | ✅ **구간 선택 기능의 전제 검증 완료** |
| `<trkseg>` | 91개는 정확히 1개(경로 끊김 없음). **`nam-03`은 0개** — 태그가 `<trkseg>`가 아니라 **`<trkseq>`로 오타**가 나 있다 | ⚠️ 파서를 관대하게 만들어 대응(원본 미수정) |
| `creator` | **3종이 섞여 있다** — GPS Track Editor 75개 / GPS Visualizer 16개 / **Korea Tourism Organization 1개(`nam-03`)** | ⚠️ 단일 출처가 아니다 |
| **고도 `<ele>`** | 92개 중 **82개가 전부 0.00**. 값이 있는 10개도 nam-88은 325 상수, nam-73/83/84는 0~18m로 무의미 → **실측 고도는 nam-23~27 5개뿐** | ⚠️ **DEM 조회 필수** |
| `<time>` | 2020-01-16~20 사이 15분 간격 = GPS Track Editor **편집 시각**. 보행 시간 아님 | ⚠️ 폐기. 소요시간은 계산값 |
| `<wpt>` | 0개 | ⚠️ 경유지 정보 없음 |
| `<trk><name>` | **시작/종료 지점명 + 지자체명 포함.** 70개가 `###-시작~종료` 표준형식 | ✅ 메타데이터 자동 추출 가능 |
| 비표준 `trk name` | 22개. 특히 **nam-03은 내용이 `"GPX"` 뿐** — 한국관광공사 원본이라 지점명이 애초에 없다 | ⚠️ 수기 보정 필요 |

**GPX 특성**: 대부분 `GPS Track Editor`·`GPS Visualizer`로 만들어졌다 — 실측 GPS 로그가 아니라 지도상에서 편집·정리된 노선이다. **좌표(경로)와 연결성은 신뢰할 수 있지만 고도와 시간은 신뢰할 수 없다.**

### 0.2 gpsroute.site — 전면 미채택 (노선·고도 모두)

요청에 따라 추출 가능성을 조사했다. 결론은 **노선 데이터를 교체할 이유가 없다**는 것이다.

**구조**: Next.js 앱. 코스 목록 페이지(`/courses/nampparang`)는 SSR로 렌더되고, 지도 데이터는 클라이언트가 `/api/courses/{slug}/shape`를 호출해 받는다.

| 확인 항목 | 결과 |
|---|---|
| 공개 HTML에서 추출 가능한 것 | **90개 코스 × (거리, 누적상승, 지역)** — 추출 완료 → `data/reference/gpsroute-ascent.json` |
| 사이트 표기 합계 | 90개 구간 · 1,429.4 km · 누적 상승 **38,070 m** (내 추출 합계와 정확히 일치) |
| 코스 상세 페이지 | **없음.** sitemap 579개 URL 중 코스 페이지는 19개(길 단위)뿐, 개별 코스 페이지 없음 |
| 소요시간 / 난이도 / 시작·종료 지점명 | **공개 페이지에 없음** |
| `shape` API 해상도 | **코스당 정확히 40점** (90코스 총 3,600점). 20km 코스면 점 간격 500m → **실사용 불가한 오버뷰 전용** |
| GPX 원본 다운로드 | `/api/` 경유 |

**추출을 중단한 두 가지 이유:**

1. **`robots.txt`가 `/api/`를 전면 차단하고 있고, `ClaudeBot`·`Claude-User`·`anthropic-ai`를 명시적으로 지정해 두었다.** 사이트 운영자가 자동화된 API 접근을 명확히 거부한 상태다. 공개 HTML 페이지는 `Allow: /`이지만 API는 아니다.

   *경위를 밝혀둔다*: `robots.txt`를 확인하기 전에 `shape` API를 1회 호출했다(구조 파악용). 확인 직후 API 호출을 중단했고, 그 이후로는 공개 HTML만 사용했다. GPX 원본은 받지 않았다.

2. **데이터 품질이 우리 것보다 낮다.** 코스별로 대조한 결과:

   | 비교 | 결과 |
   |---|---|
   | 총 거리 | 우리 1,463.9 km vs gpsroute 1,429.9 km (2.4% 차) |
   | 오차 8% 이내 | 72 / 90 코스 |
   | 큰 불일치 | **23코스** 우리 17.10 km vs 저쪽 9.00 km (+90%) / **54코스** 15.56 vs 7.30 (+113%) / **59코스** 11.98 vs 7.80 (+54%) |
   | 경계 차이인가? | ❌ 아니다. 인접 3코스 묶음으로 봐도 22~24구간 +27.4%, 53~55구간 +27.5%로 **차이가 상쇄되지 않는다** |
   | 10코스 단위 | 21~30구간 +6.7%, 51~60구간 +9.9%를 제외하면 모두 0.2~3.6% 일치 |

   즉 코스 분절 차이가 아니라, gpsroute 쪽 **23·54·59코스 트랙이 잘려 있다.** 남파랑길 코스는 통상 10~25km인데 7~9km는 비정상이다. 우리 Daum GPX가 더 완전하다.

**결론**: 노선은 Daum GPX 유지. 누적상승 90개는 처음엔 고도 대조군으로 쓸 생각이었지만, **단계 2a에서 유효성을 검증해 보니 약 1.3배 과대계상이었다**(3.1.1 참조). `data/reference/`에 외부 참고자료로만 분리 보관하고 캘리브레이션 기준으로는 쓰지 않는다. 출처는 README와 `data/SOURCE.md`에 표기한다.

> ⚠️ **이 절의 "gpsroute 트랙이 잘려 있다" 판단은 틀렸다. 0.4 참조.**
> gpsroute 는 공식 데이터와 90/90 일치했고, 우리 Daum GPX 가 이상치였다.
> 노선을 Daum GPX 로 유지한다는 결론 자체는 유효하지만(전체 좌표를 가진 유일한 자료),
> 근거로 든 "우리 데이터가 더 완전하다"는 사실이 아니다.

> 요청하신 "1번이 가능하면 그걸로 구현" 조건은 성립하지 않는다. 기술적으로는 일부 추출이 가능하지만, 얻는 노선 데이터가 이미 가진 것보다 나쁘고 API 접근은 운영자가 거부한 상태다.

### 0.4 두루누비(한국관광공사) — 코스 메타데이터 채택, **그리고 앞선 판단 정정**

`https://www.durunubi.kr/namparang-course-list.do` 의 목록 API(`/api/course/list`)에서
90개 코스의 공식 메타데이터를 받았다. `robots.txt` 가 차단한 `/api/` 경로에 포함되지
않으며, 차단된 `/download`(GPX)는 받지 않았다. 요청 3회.

얻은 것: 시점·종점 지점명 / 주소 / **교통편** / 시군 / 공식 거리·소요시간·난이도 / 설명.
`travelerInfo` 필드 파싱 **90/90 성공**. 이걸로 비어 있던 13개 코스를 포함해
**90개 전부의 지점명·주소·교통편·지역을 채웠다.**

#### ⚠️ 0.2 의 결론을 정정한다

0.2 에서 나는 gpsroute 의 23·54·59코스 트랙이 **잘려 있다**고 판단하고, 그 근거로
"남파랑길 코스는 통상 10~25km인데 7~9km는 비정상"이라고 적었다. **틀렸다.**
공식 데이터와 대조한 결과:

| | 총거리 | 공식과 차이 | 코스별 8% 이내 일치 |
|---|---|---|---|
| 두루누비(공식) | 1,426.8 km | — | — |
| gpsroute | 1,429.9 km | +3.1 | **90 / 90** |
| **우리 Daum GPX** | **1,464.4 km** | **+37.6** | **74 / 90** |

| 코스 | 공식 | gpsroute | 우리 GPX |
|---|---|---|---|
| 23 | 8.8 | 9.0 | **17.1** |
| 54 | 7.5 | 7.3 | **15.6** |
| 59 | 8.2 | 7.8 | **12.0** |

gpsroute 가 공식과 맞았고 **우리 GPX 가 이상치**였다. 내가 "우리 데이터가 더 완전하다"고
결론 낸 것은 검증 대조군 없이 코스 길이에 대한 내 통념을 기준으로 삼은 탓이다.

#### 우리 GPX 가 공식과 다른 이유 (결함은 아니다)

왕복·중복 구간 탐지를 돌렸다 — 시퀀스상 1km 이상 떨어진 두 점이 공간적으로 50m 이내인
비율이 **0%**(정상 코스와 동일). 트랙이 스스로를 되짚지 않는다. 즉 파일 결함이 아니라
**같은 두 지점을 잇는 다른(더 긴) 노선**이다. `trk name` 에 `노선수정`·`우회노선활용`·
`시종점변경` 이 적혀 있던 것과 일치한다.

공식보다 3km 이상 긴 코스: 23(+8.3), 54(+8.1), 59(+3.8), 76(+3.1), 80(+3.9) — 합 +27.2km.

#### 경계가 다른 14개 슬롯은 공식 지점명을 쓰지 않았다

공식 코스 경계와 우리 트랙 끝점의 거리가 500m를 넘는 곳:

| 접합부 | 차이 | | 접합부 | 차이 |
|---|---|---|---|---|
| 23종점/24시점 | 617m | | 60종점/61시점 | 1,389m |
| 41종점/42시점 | 1,811m | | 76종점/77시점 | 1,593m |
| 53종점/54시점 | **7,366m** | | 87종점/88시점 | 2,920m |
| 58종점/59시점 | 2,785m | | | |

이 슬롯에는 기존 GPX `trk name` 값을 유지했다(12개). 대체값이 없던 41종점·42시점만
공식값을 쓰고 `note` 에 경계 차이를 기록했다. 해당 14개 코스는 앱에서 메모로 확인된다.

### 0.5 출처 정리 — 어디까지 공식 자료로 대체했나 (2026-08-27)

**노선 좌표만 카페 자료를 쓰고, 나머지는 전부 공식 자료로 교체했다.**

| 항목 | 출처 | 근거 |
|---|---|---|
| 노선 좌표 | Daum 카페 | 대체 불가 (아래) |
| 시점·종점 지점명 | 두루누비 | 90/90 확보 |
| 주소 · 교통편 | 두루누비 | 90/90 확보 |
| 지역(시·군·구) | 두루누비 `sigun` | 20개 그룹 |
| **난이도** | 두루누비 `lev` | 내 공식과 41/90만 일치 — 운영기관 등급이 신뢰도 높음 |
| **소요시간** | 두루누비 | 공식 권장 514시간 (내 계산 415시간) |
| 거리 | **우리 GPX 실측** | 지도에 그리는 선과 숫자가 어긋나면 안 된다 |
| 고도 | SRTM 30m DEM | 원본 GPX에 사실상 없음 |

#### 노선 좌표는 왜 대체할 수 없는가

| 경로 | 결과 |
|---|---|
| `gpx_src` (`/data/koreamobility/course/summap/*.gpx`) | **HTTP 404** |
| 상세 페이지의 실제 다운로드 (`/download?filePath=...`) | **`robots.txt` 가 `/download` 차단** |
| 목록 API 의 좌표 | **시작·종료 4개 값뿐** — 폴리라인 없음 |

앱은 92개 코스 3,886점(오버뷰) + 744KB(상세+고도)의 좌표를 쓴다.
공식 자료만으로는 **지도에 선을 하나도 그릴 수 없다.**

노선을 공식 것으로 바꾸려면 사용자가 두루누비에서 직접 GPX 를 내려받아
`data/gpx/` 에 넣으면 된다. 파이프라인은 파일명만 맞으면 그대로 동작한다.

#### 라이선스 — 대체가 오히려 제약을 늘린다

두루누비 자료는 **공공누리 제4유형(출처표시 + 상업적이용금지 + **변경금지**)** 이다.
상세 페이지에 명시돼 있다. Daum 카페 자료는 라이선스 표기가 **없다**(모호).

즉 "라이선스가 불분명한 자료를 공식 자료로 바꾸면 깔끔해진다"는 기대와 반대로,
공식 자료 쪽에 **명시적인 변경금지 조건**이 붙는다. 이 프로젝트는 공식 텍스트를
파싱해 필드로 재구성하고 공개 리포에 올린다 — 그것이 "변경"인지는 해석의 문제이고
나는 법률 판단을 할 위치가 아니다. 사실만 기록한다: 출처를 표기했고, 상업적 이용을
하지 않으며, 원본 응답을 `data/reference/durunubi-courses.json` 에 보관했다.

### 0.3 검증이 바꾼 결정

1. **고도는 필수 전처리 단계다.** DEM 조회를 전 코스 기본 경로로 승격. 대조군은 실측 고도가 남아있는 `nam-23`~`nam-27` 5개 코스뿐이다.
2. **소요시간은 100% 계산값이다.** GPX `<time>` 미사용. UI에 "예상 소요시간"으로 명시.

---

## 1. 설계 원칙

| 원칙 | 결정 |
|---|---|
| 최대한 가벼운 스택 | 빌드 도구 1개(Vite) + 지도 1개(Leaflet). 프레임워크 없음(Vanilla JS + ES 모듈) |
| **서버·외부 서비스 0개** | 코스 데이터는 정적 JSON. 사진/메모는 **GitHub 리포 자체가 DB** |
| 오프라인 우선 | IndexedDB가 1차 저장소, GitHub는 동기화 대상. 해안·산간에서 네트워크 없이도 기록 가능 |
| 무료 호스팅 이식성 | 상대 경로 + 단일 `dist/`. GitHub Pages / Cloudflare Pages / Netlify 어디든 그대로 |
| 유지관리 | 원본 GPX·전처리 스크립트·산출 JSON·검증 결과·사진·메모가 **전부 git 안에** |

**성능 예산**: 랜딩 첫 로딩 JS ≤ 80KB(gzip), 초기 데이터 ≤ 300KB, 코스 상세는 lazy fetch.

**단계 6b 실측**: JS **61.6KB** gzip (Leaflet 42 + 자체 코드 20) · CSS **10.2KB** ·
`exifr` **14.6KB** (사진 등록 시에만 지연 로딩) · `courses.json` 전송 **37.5KB** gzip.
랜딩 예산(80KB) 안이다. 차트 라이브러리 의존성 0.

---

## 2. 기술 스택

| 영역 | 선택 | 크기(gzip) | 비고 |
|---|---|---|---|
| 빌드 | Vite 6 | (dev only) | 정적 빌드, 설정 10줄 |
| 지도 | Leaflet 1.9 | ~42KB | 래스터 타일. MapLibre보다 가볍고 이 용도엔 충분 |
| 타일 | OSM 기본 / OpenTopoMap(지형) / Esri World Imagery(위성) 3종 토글 | 0 | 개인 트래픽 수준. 필요 시 VWorld 키로 교체 |
| GPX 파싱 | 빌드 시 `fast-xml-parser` (Node) | 0 (런타임 미포함) | 브라우저에 파서를 싣지 않는다 |
| 좌표 단순화 | 자체 Douglas–Peucker (~30줄) | 0 | turf 의존성 회피 |
| 고도 프로필 | 자체 인라인 SVG polyline | ~2KB | 차트 라이브러리 미사용 |
| 사진 EXIF | `exifr` (GPS 서브셋) | ~10KB | 동적 import |
| 로컬 저장 | IndexedDB (자체 얇은 래퍼) | ~1KB | 오프라인 1차 저장소 + 동기화 큐 |
| **원격 저장** | **GitHub Contents API (`fetch`)** | ~3KB (자체 코드) | 신규 의존성 0. SDK·서버·외부 계정 불필요 |
| 백업 ZIP | `fflate` | ~8KB | 동적 import |

> 런타임 의존성은 Leaflet(항상) + exifr/fflate(필요 시)뿐. GitHub 연동은 자체 작성 100줄 내외.

---

## 3. 데이터 파이프라인

GPX는 **빌드 타임에 1회 전처리**해 JSON으로 굽는다. 브라우저는 GPX를 절대 파싱하지 않는다.

```
data/gpx/nam-01.gpx ... nam-90.gpx (+17-1, 61-1)   [92개, 14.4MB, 확보 완료]
        |
        +-- npm run build:ele        (완료) DEM 고도 조회 -> data/cache/ele/*.json  [커밋]
        +-- npm run calibrate:ele    (완료) nam-23~27 실측 대조 -> 창3/3m 확정
        v  npm run build:data    (scripts/build-data.mjs, Node)
        |
        +--> public/data/courses.json        # 92개 메타 + 저해상도 오버뷰 (~250KB)
        +--> public/data/course/{id}.json    # 상세 좌표 + 고도 (~15-35KB x 92, lazy)
        +--> public/data/meta.json           # 생성일시, 출처, 총거리, 체크섬
```

### 3.1 고도 확보 (DEM) — ✅ 완료 (2026-08-27)

134,697점 전체를 조회하지 않는다. **50m 균일 간격으로 재샘플링해** 조회한다.
원본 트랙포인트는 간격이 불규칙해서(곡선부 촘촘, 직선부 드문드문) 그대로 쓰면
곡선부 DEM 노이즈가 누적 상승을 부풀린다. 균일 간격이어야 고도 프로필 x축도 거리에 정비례한다.

**실행 결과**

| 항목 | 값 |
|---|---|
| API | opentopodata 공개 API (무료·키 불필요), `srtm30m` |
| 보간 | `cubic` — 실측 대조에서 nearest 3.2m / bilinear 3.4m 대비 **2.8m**로 가장 정확 |
| 샘플 | **29,660점** (92개 코스) |
| 요청 | **328회** (일일 한도 1,000 안쪽) |
| DEM 공백(null) | **0점** |
| 캐시 | `data/cache/ele/` 1.1MB — 커밋했으므로 재조회 없음, 오프라인 빌드 가능 |

**DEM 절대 정확도** (실측 고도가 남아있는 nam-23~27 대조)

| 코스 | n | 평균절대오차 | 편향 |
|---|---|---|---|
| 23 | 344 | 6.1m | −0.4m |
| 24 | 228 | 5.7m | +1.6m |
| 25 | 291 | 5.2m | −0.2m |
| 26 | 262 | 6.9m | +2.8m |
| **27** | 204 | **38.4m** | −22.1m ⚠️ 이상치 |

이상치 제외 4개 코스 **평균절대오차 6.0m**. 통상 인용되는 SRTM 전역 오차 ±16m보다 한국 연안에서는 양호하다.
`nam-27`은 편차 표준편차가 49.3m로 일정 오프셋이 아니라 **노선과 고도 시리즈가 어긋난** 형태다. 원인 미확정.

**누적 상승 파라미터** — SRTM은 정수 미터만 주므로 평지에서도 ±1m 계단이 끝없이 생긴다.

| 설정 | DEM/실측 비율 | 90코스 합계 |
|---|---|---|
| 창1 / 0m (무처리) | **1.22** | 47,568m (32.5 m/km) ← 양자화 노이즈로 부풀려짐 |
| **창3 / 3m** | **1.06** | **29,220m (20.0 m/km)** ← 채택 |
| 창5 / 3m | 1.04 | 25,527m (17.4 m/km) |
| 창3 / 8m | 1.02 | 24,354m (16.6 m/km) ← 실제 기복까지 지움 |

**창3/3m 채택.** 비율이 1.0에 더 가까운 설정도 있지만, 그것들은 실제 지형의 100m 규모 기복까지
깎아내면서 우연히 맞는 것이다. 창3/3m은 양자화 노이즈를 제거하는 가장 약한 처리다.
상수는 `scripts/lib/config.mjs`에 근거와 함께 고정했고, `scripts/calibrate-ele.mjs`로 재현된다.

**UI 표기**: "고도는 DEM 추정값" 각주 필수. 누적 상승은 참고값이다.

### 3.1.1 gpsroute 대조군은 폐기했다 — 과대계상

원래 gpsroute의 코스별 누적상승(합계 38,070m)을 두 번째 대조군으로 쓸 계획이었다.
**유효성을 직접 검증한 결과 절대 기준으로 쓸 수 없다.**

실측 고도가 있는 코스에서, **DEM을 거치지 않은 실측 GPX 자체의 누적상승**을
가장 부풀려지는 설정(창1/0m)으로 계산해 저쪽 값과 비교했다:

| 코스 | 우리 거리 | 저쪽 거리 | 실측 상승 | 저쪽 상승 | 실측/저쪽 |
|---|---|---|---|---|---|
| 23 | 17.10km | 9.00km (+90%) | 1,141m | 666m | — 트랙 잘림, 비교 무효 |
| 24 | 11.31km | 10.50km | 267m | 416m | **0.64** |
| 25 | 14.50km | 14.50km | 342m | 394m | **0.87** |
| 26 | 13.03km | 13.00km | 396m | 540m | **0.73** |
| 27 | 10.12km | 10.10km | 272m | 339m | **0.80** |

거리가 0.2% 이내로 일치하는 = **같은 노선인데도** 실측 누적상승이 저쪽 값의 평균 **76%**다.
저쪽이 약 1.3배 부풀려져 있다는 뜻이고, 아마 스무딩 없이 촘촘한 샘플로 계산했을 것이다.

→ `data/reference/`에 참고자료로 남기되 **캘리브레이션 기준으로는 쓰지 않는다.**
파라미터는 실측 5개 코스만으로 정했다. 우리 값 20.0 m/km가 저쪽 26 m/km보다 실제에 가깝다.

### 3.2 코스별 산출 지표 — ✅ 완료

| 지표 | 산출 방법 |
|---|---|
| 총길이 | 하버사인 누적 (단순화 **전** 원본 해상도) — 검증 완료 |
| 누적 상승/하강 | DEM 고도 → 이동평균 → ±3m 임계값 합산 (3.1) |
| 예상 소요시간 | 네이스미스 변형: `거리/4km/h + 상승m/600m/h`, 10분 반올림. **GPX `<time>` 미사용** |
| 난이도 | `점수 = 거리km + 상승m/100` → **≤16 쉬움 / ≤22 보통 / >22 어려움**. 경계는 실제 분포(최소 10.1 / 중앙 18.9 / 최대 30.8)에 맞춰 정했다. 처음 잡았던 10/16은 코스가 이렇게 길 줄 모른 추측값이라 90개 중 73개가 '어려움'으로 몰렸다 → 쉬움 17 / 보통 48 / 어려움 25 |
| 시작/종료 지점명 | `<trk><name>` 파싱 **75개 자동** → 연쇄 보정(앞 코스 종점=다음 코스 시점) **4개 추가** → 접합부 라벨 통일 7곳 → CSV 보정. **14개는 원본에 정보가 없어 비워둠**(1,2,3,36~46) |
| 지역(시·군) | 지점명 앞부분에서 추출 → **17개 지역, 90코스 전부 확보**. 부산 6 / 남해군 11은 좌표로 확인해 CSV 보정 |
| 오버뷰 라인 | Douglas–Peucker **ε=60m → 코스당 평균 43점** (전체 3,859점, 79KB) |
| 상세 라인 | **ε=4m → 코스당 평균 253점** (ε8m으로 줄여도 코스당 3KB만 아끼면서 최대 줌에서 오차가 보인다) |
| 좌표 정밀도 | 소수점 5자리(≈1m) 절단 → JSON 약 30% 축소 |

### 3.3 메타데이터 보정 레이어

자동 파싱되지 않는 22개(특히 내용이 `"GPX"`뿐인 **nam-03**)는 `data/courses.meta.csv`로 덮어쓴다.

```csv
id,name,region,start,end,difficulty,note
3,3코스,부산 사하구,<수기입력>,<수기입력>,,trk name 누락
17-1,17-1코스(임시노선),경남 거제,고현,옥포,,임시우회노선
61-1,61-1코스(우회),전남 순천,,,,흑두루미 보호기간(10월말~4월) 우회로
```

빌드는 `GPX 파싱값 → CSV 값` 순으로 덮어쓰므로 **재빌드해도 수기 보정이 유지된다.**

### 3.4 courses.json 스키마(안)

```json
{
  "id": "17", "seq": 17, "name": "17코스", "region": "경남 거제시",
  "start": "거제시 고현버스터미널", "end": "거제시 장목면 장목파출소",
  "distanceKm": 19.14, "durationMin": 320,
  "ascentM": 412, "descentM": 398, "eleMin": 3, "eleMax": 268,
  "eleSource": "srtm30",
  "difficulty": "중",
  "isAlt": false,
  "bbox": [128.1, 34.9, 128.4, 35.1],
  "startLatLng": [35.02, 128.15],
  "overview": [[35.02, 128.15], [35.03, 128.16]]
}
```

`isAlt: true`인 `17-1`, `61-1`은 **구간 합계에서 제외**하고 지도에는 점선으로 표시한다.

---

## 4. 화면 구성

```
+------------------------------------------------------+
| =  남파랑길              [지도 v] [토큰 설정] [동기화]|
+------------+-----------------------------------------+
| 사이드메뉴 |                                         |
|            |            지 도  (Leaflet)             |
| 시작 [1 v] |                                         |
| 종료 [90v] |   - 90개 코스 폴리라인 (난이도별 색)    |
| [전체보기] |   - 선택 구간 강조 / 그 외 흐리게       |
|            |   - 임시노선은 점선                     |
| -- 요약 -- |   - 코스 시작점 마커                    |
| 코스 12개  |   - 사진 핀 (클릭 -> 이미지+메모 팝업)  |
| 178.4 km   +-----------------------------------------+
| 43시간 20분|   고도 프로필 (선택 구간, 인라인 SVG)   |
| 상승 3,120m|   호버 -> 지도에 현재 지점 커서 동기화  |
| 난이도 중  +-----------------------------------------+
|            | (i) 코스 정보 카드                      |
| -- 코스 -- |     거리 / 예상시간 / 난이도 / 고도     |
| > 17코스   |     * 고도는 DEM 추정값                 |
| > 18코스   |                                         |
| [+ 사진]   |                                         |
+------------+-----------------------------------------+
```

- **랜딩**: `courses.json` 하나만 받아 92개 오버뷰 라인 전체 표시 + 전체 bbox `fitBounds`.
- **구간 선택**: 시작/종료 `<select>`(1~90). 해당 범위 상세 JSON 병렬 fetch(메모리 캐시) → 강조 렌더 + 합계 재계산 + `fitBounds`. **접합부가 모두 500m 이내로 검증됐으므로 단순 연결로 충분하다.**
- **URL 상태 동기화**: `?from=17&to=23&layer=topo`
- **반응형**: 모바일에서 사이드바 → 바텀시트(3단 스냅), 고도 프로필 기본 접힘
- **편의**: 키보드 좌/우 코스 이동, 다크모드

---

## 5. 사진 + 메모 — GitHub을 백엔드로 쓴다

### 5.1 Issues / Wiki / Contents API 비교

요청하신 Issues·Wiki 활용 가능성을 전부 검토했다.

| | **Issues** | **Wiki** | **Discussions** | **Contents API** |
|---|---|---|---|---|
| 브라우저에서 쓰기 | ✅ REST | ❌ **git push만 가능**, 쓰기 REST API 없음 | ✅ GraphQL | ✅ REST |
| 인증 없이 읽기 | ⚠️ API 60req/hr | ✅ raw | ⚠️ 토큰 필요 | ✅ **raw, 한도 없음** |
| **이미지 업로드** | ❌ 첨부 API 없음 (세션 인증 필요한 비공개 엔드포인트) | ❌ | ❌ | ✅ **base64로 직접 커밋** |
| 구조화 조회 | 라벨/검색 | ❌ | 라벨 | JSON 파싱 |
| git 히스토리 부풀림 | 없음 | 있음 | 없음 | 있음 |

**결론: Contents API가 정답이다.** Issues와 Wiki가 탈락하는 이유는 하나로 같다 — **이미지를 올릴 방법이 없다.** GitHub에서 이슈 본문에 이미지를 끌어다 놓을 때 쓰는 업로드 엔드포인트는 공개 API가 아니고 브라우저 세션 인증을 요구한다. PAT로는 호출할 수 없다. 결국 이미지는 리포에 커밋해야 하고, 그러면 Issues는 "메모만 담는 불필요한 중간 계층"으로 남는다. Wiki는 애초에 REST 쓰기 API 자체가 없어서 브라우저에서 불가능하다.

### 5.2 검증한 사실 (실측)

```
raw.githubusercontent.com   →  Access-Control-Allow-Origin: *  /  Cache-Control: max-age=300
cdn.jsdelivr.net (gh)       →  Access-Control-Allow-Origin: *  /  s-maxage=43200 (12시간)
api.github.com 미인증        →  60 req/hr
```

- **읽기 경로는 `raw.githubusercontent.com`으로 간다.** CORS가 열려 있고, 5분 캐시라 갓 올린 사진도 곧 보이며, **API 60req/hr 한도에 걸리지 않는다.**
- jsDelivr는 CORS는 열려 있지만 **12시간 캐시**라 방금 올린 사진이 안 보인다. 쓰지 않는다.
- 쓰기는 인증되므로 5,000 req/hr — 개인 사용엔 무제한이다.

### 5.3 아키텍처

```
[브라우저]                                    [GitHub]
IndexedDB ---- 동기화 큐 ----> Contents API --- namparang       (메모 JSON)
(1차 저장소)                                 --- namparang-photos (사진, 별도 리포)
          <--- raw.githubusercontent.com ---
```

| 대상 | 위치 | 이유 |
|---|---|---|
| 메모·좌표 | `namparang` 리포 `data/notes/{noteId}.json` — **노트 1개당 파일 1개** | 단일 `notes.json`은 동시 수정 시 SHA 충돌이 잦다. 파일을 쪼개면 충돌이 사라진다 |
| 사진 | **별도 리포 `namparang-photos`** `{yyyy}/{noteId}.jpg` + `_t.jpg`(썸네일) | 소스 리포를 가볍게 유지 — `git clone`이 무거워지면 "유지관리" 제약에 어긋난다 |
| 인덱스 | 빌드 시 `data/notes/*.json`을 합쳐 `public/data/notes.json` 생성 | 방문자는 파일 1개만 받는다 |

**IndexedDB가 1차 저장소다.** 등산 중 네트워크가 없어도 등록이 즉시 성공하고, 연결되면 큐가 자동으로 밀려 올라간다.

### 5.4 인증 — 정적 사이트의 근본 문제와 유일한 해법

Public 리포의 클라이언트 사이드 앱에는 **비밀을 숨길 수 없다.** 검토한 선택지:

| 방안 | 결과 |
|---|---|
| PAT를 리포에 커밋 | ❌ 누구나 리포에 쓸 수 있게 된다 |
| GitHub OAuth (표준) | ❌ 토큰 교환에 `client_secret` 필요 → 서버 필요 |
| **OAuth Device Flow** | `client_secret`이 없어도 되지만, GitHub의 토큰 엔드포인트가 **CORS를 허용하지 않아** 브라우저에서 직접 호출할 수 없다 → 프록시(=서버) 필요 → ❌ |
| GitHub Actions 경유 | ❌ 트리거에 여전히 토큰 필요 |
| **본인 PAT를 브라우저에만 보관** | ✅ **유일하게 서버가 필요 없는 해법** |

**채택안**: 사용자가 fine-grained PAT를 앱에 한 번 붙여넣고 `localStorage`에 보관한다. **리포에는 비밀이 전혀 없다.** 다른 방문자는 읽기 전용이고, 사용자 본인 브라우저에서만 쓰기가 가능하다.

위험과 완화 — 정직하게 적는다. **PAT를 `localStorage`에 두면 XSS가 터졌을 때 토큰이 유출된다.**

- 서드파티 스크립트를 **0개**로 유지 (분석 도구·광고·CDN 스크립트 없음)
- 메모는 **절대 `innerHTML`로 렌더하지 않는다** — `textContent`만 사용
- PAT 권한을 **해당 리포 2개의 Contents 쓰기로만** 제한 (fine-grained PAT). 계정 전체 권한을 주지 않는다
- 만료를 90일로 설정
- CSP 메타 태그로 인라인/외부 스크립트 차단

권한이 리포 2개의 파일 쓰기로 한정되므로, 최악의 경우 피해는 "이 두 리포가 오염된다"이고 git이므로 복구된다. 개인 프로젝트에서 받아들일 만한 수준이라고 판단한다.

### 5.5 등록 흐름

1. `[+ 사진]` → 파일 선택(다중, 모바일 카메라 촬영 가능)
2. `exifr`로 EXIF GPS + 촬영일시 추출
   - GPS 있음 → 그 좌표 사용, 가장 가까운 코스에 자동 매핑
   - GPS 없음 → **지도 클릭으로 위치 지정** fallback (필수 UX — 메신저/SNS 경유 사진은 EXIF가 지워진다)
3. Canvas 리사이즈 **최대변 1280px / JPEG 0.75 (≈150KB)** + 썸네일 256px(≈20KB) → 원본 폐기
4. 메모 입력 → IndexedDB 저장 + 큐 등록 → 온라인이면 즉시 커밋
5. 지도에 핀 표시 → 팝업(썸네일+요약) → 모달(원본, 좌우 넘김)

### 5.6 용량 — 이게 유일한 실질 제약

**커밋된 사진은 삭제해도 git 히스토리에 영구히 남는다.** 이건 우회할 수 없는 성질이다.

| 규모 | 리포 크기 | 판단 |
|---|---|---|
| 90코스 × 5장 = **450장** | 약 77 MB | ✅ 현실적 목표. 문제 없음 |
| 1,500장 | 약 255 MB | ✅ 여유 |
| 3,000장 | 약 510 MB | ⚠️ GitHub 권장 상한(1GB)의 절반. 경고 신호 |
| 6,000장 이상 | 1 GB 초과 | ❌ 이전 필요 |

- 사진 전용 리포를 분리하므로 **소스 리포는 항상 20MB 미만**으로 유지된다
- 앱에 "사진 N장 · 추정 M MB" 표시를 넣어 한도를 눈으로 보게 한다
- 상한에 닿으면 → Cloudflare R2(10GB 무료)로 이전. 동기화 계층이 자체 코드 100줄이라 교체가 저렴하다

### 5.7 Supabase를 폐기하는 이유

| 항목 | GitHub Contents API | Supabase |
|---|---|---|
| 외부 계정 | **불필요** (이미 쓰는 GitHub) | 추가 필요 |
| 서버/DB 운영 | 없음 | 관리형 Postgres |
| 무활동 중지 | **없음** | ⚠️ 7일 → keepalive cron 필요 |
| 백업 | **git 자체가 백업**, 이력 전체 보존 | 별도 export 필요 |
| 용량 | 약 3,000장(경고선) | 1GB ≈ 3,000장 |
| 인증 | ⚠️ 브라우저 보관 PAT | ✅ 정식 Auth + RLS |
| 유지관리 지점 | **1곳 (GitHub)** | 2곳 |

인증 방식만 Supabase가 낫고, **나머지 전부 GitHub이 낫다.** 특히 "최대한 가벼운 스택 / 개인 사용 / github로 유지관리"라는 세 가지 제약에 정면으로 부합한다. keepalive cron이 사라지고, 계정이 하나 줄고, 백업이 공짜로 따라온다.

**ZIP export/import는 유지한다** — GitHub 계정 문제나 리포 사고에 대한 탈출구다.

---

## 6. 리포지토리 구조

```
namparang/                          (Public, GitHub Pages)
├─ index.html                       # CSP 메타 태그 포함
├─ package.json / vite.config.js
├─ README.md                        # 스크린샷, 실행법, 데이터 출처·저작자 표기
├─ PLAN.md
├─ .github/workflows/deploy.yml     # push -> build -> Pages
├─ data/
│  ├─ gpx/nam-01.gpx ... nam-90.gpx, nam-17-1, nam-61-1   [92개 완료]
│  ├─ raw/attachments.json          # Daum 첨부 목록 + URL          [완료]
│  ├─ raw/downloaded.json           # 파일명/크기/SHA1              [완료]
│  ├─ raw/validate.json             # 전수 검증 결과                [완료]
│  ├─ reference/gpsroute-ascent.json# 외부 참고 누적상승 90개       [완료]
│  ├─ courses.meta.csv              # 수기 보정 레이어 (22개)
│  ├─ cache/ele/nam-NN.json         # DEM 고도 캐시
│  ├─ notes/{noteId}.json           # 메모 (앱이 커밋)
│  └─ SOURCE.md                     # 출처·수집일·이용조건·원저작자
├─ scripts/
│  ├─ fetch-gpx.mjs                 # Daum 카페 첨부 수집 (재현용)
│  ├─ build-ele.mjs                 # DEM 조회 + gpsroute 대조 검증
│  ├─ build-data.mjs                # GPX -> JSON 전처리
│  └─ build-notes.mjs               # data/notes/*.json -> public/data/notes.json
├─ public/data/                     # 생성물 (커밋 O)
└─ src/
   ├─ main.js / map.js / courses.js / sidebar.js / elevation.js
   ├─ photos.js                     # EXIF, 리사이즈, 핀/팝업
   ├─ store.js                      # IndexedDB + 동기화 큐
   ├─ github.js                     # Contents API (fetch, SDK 없음)
   ├─ token.js                      # PAT 입력/보관/검증
   ├─ backup.js                     # ZIP export/import
   └─ style.css

namparang-photos/                   (Public 또는 Private, 사진 전용)
└─ {yyyy}/{noteId}.jpg, {noteId}_t.jpg
```

---

## 7. 단계별 실행 계획

| 단계 | 내용 | 산출물 | 예상 |
|---|---|---|---|
| ~~1. 데이터 확보~~ | ~~GPX 92개 수집·검증, 외부 참고자료 조사~~ | ✅ **완료** | ~~1일~~ |
| ~~0. 준비~~ | ~~`data/SOURCE.md`, git init, Vite 스캐폴딩, 리포 생성, Pages+Actions~~ | ✅ **완료** — https://yeonkyupark.github.io/KDT-Namparang/ | ~~0.5일~~ |
| ~~2a. 고도 확보~~ | ~~opentopodata 조회, 캐시 커밋, 임계값 캘리브레이션~~ | ✅ **완료** — 29,660점 / 328요청 / null 0. 창3/3m 채택, DEM/실측 1.06 | ~~1일~~ |
| ~~2b. 전처리~~ | ~~파싱·단순화·거리/고도/시간/난이도, 지점명 보정~~ | ✅ **완료** — `courses.json` 118KB, `course/*.json` 코스당 8.1KB | ~~1일~~ |
| ~~3. 랜딩 지도~~ | ~~Leaflet, 92개 전체 표시, 타일 토글, 시작점 마커, 코스 정보 카드~~ | ✅ **완료** — JS 47.2KB gzip / CSS 7.8KB / 데이터 32.6KB gzip | ~~1일~~ |
| ~~4. 구간 선택 + 정보~~ | ~~사이드바, 시작/종료 select, 강조 렌더, 요약, URL 동기화, `isAlt` 합계 제외, 모바일 바텀시트~~ | ✅ **완료** — JS 48.0KB gzip / CSS 8.3KB | ~~1.5일~~ |
| ~~5. 고도 프로필~~ | ~~인라인 SVG 차트, 지도 커서 동기화, "DEM 추정값" 각주~~ | ✅ **완료** — JS 50.2KB gzip / CSS 8.5KB | ~~0.5일~~ |
| ~~6a. 사진/메모 (로컬)~~ | ~~IndexedDB, EXIF, 리사이즈, 핀/모달, 지도클릭 fallback~~ | ✅ **완료** — JS 55.8KB gzip + exifr 14.6KB(지연 로딩) | ~~2일~~ |
| **6b. GitHub 동기화** | ~~`github.js`/`settings.js`/`sync.js`, PAT UI, Contents API, raw 읽기~~ | ⏳ **구현 완료 · 인증 경로 미검증** — 사진 리포와 PAT 가 있어야 실제 push/pull 확인 가능 | 1일 |
| ~~7. 백업 + 마감~~ | ~~ZIP export/import, CSP, 캐시 버스터, 접근성 점검, README~~ | ✅ **완료** | ~~1일~~ |

남은 작업량 약 **9.5일** (Supabase 폐기로 0.5일 단축). 각 단계 끝에 커밋 + 배포하여 **항상 동작하는 상태**를 유지한다.

---

## 8. 리스크 및 대응

| 리스크 | 상태 | 대응 |
|---|---|---|
| ~~GPX 확보 실패~~ | ✅ 해소 | 92개 전부 확보·검증 |
| ~~코스 연결성 불량~~ | ✅ 해소 | 접합부 89곳 모두 500m 이내 |
| ~~고도 대조군 없음~~ | ✅ 해소 | 실측 5개 코스(nam-23~27)로 캘리브레이션 완료 |
| ~~Supabase 7일 중지~~ | ✅ 해소 | Supabase 폐기 |
| **DEM 고도 정확도** | ⚠️ 잔존 (계량됨) | 실측 대조 결과 절대 고도 평균절대오차 **6.0m**, 누적상승 **실측의 1.06배**. UI에 추정값 각주 표기. 누적 상승은 **참고값**이다 |
| `nam-27` 고도 이상 | ⚠️ 원인 미확정 | 평균절대오차 38.4m, 편차 표준편차 49.3m — 오프셋이 아니라 노선/고도 시리즈 어긋남. 캘리브레이션에서 이상치로 분리했다 |
| **PAT XSS 유출** | ⚠️ 잔존 | 서드파티 스크립트 0개 + `textContent`만 사용 + CSP + fine-grained 권한 2리포 한정 + 90일 만료 |
| **git 히스토리 영구 누적** | ⚠️ 구조적 | 사진 전용 리포 분리 + 1280px/150KB + 앱 내 용량 표시. 1GB 근접 시 R2 이전 |
| ~~opentopodata 한도/장애~~ | ✅ 해소 | 328요청으로 완료, 캐시 커밋됨. 재조회 불필요 |
| nam-03 등 22개 메타 누락 | ⚠️ | `courses.meta.csv` 수기 보정 |
| **GPX 저작자 표기** | ⚠️ | Daum 카페 원저자가 90코스를 직접 걸어 정리한 자료다. `data/SOURCE.md`·README에 **출처·원저자·수집일 명시**. 재배포가 부담되면 GPX를 리포에서 빼고 링크만 남기는 것도 검토 |
| gpsroute 자료 이용 | ✅ 정리됨 | 검증 결과 **과대계상(약 1.3배)** 으로 확인돼 캘리브레이션 기준에서 배제. `data/reference/`에 참고자료로만 보관 |
| OSM 타일 정책 | 낮음 | 개인 사용 수준이면 무해. 커지면 VWorld/MapTiler 키로 교체(1줄) |
| GitHub Pages 하위 경로 | 낮음 | `vite.config.js`의 `base` 상대 경로, 데이터 fetch 전부 상대 경로 |

---

## 9. 현재 상태 / 다음 액션

**리포**: https://github.com/yeonkyupark/KDT-Namparang
**배포**: https://yeonkyupark.github.io/KDT-Namparang/ (push → Actions → Pages 자동)

단계 0 완료. 다음은:

전체 단계 완료 (0, 2a, 2b, 3, 4, 5, 6a, 6b, 7).

**남은 것 하나** — GitHub 동기화의 **인증 경로 실측**. 사진 리포
(`KDT-Namparang-photos`)와 fine-grained PAT 가 있어야 실제 push/pull 을 확인할 수 있다.
인증이 필요 없는 경로는 전부 검증했다(6b 메모 참조).

**의도적으로 넣지 않은 것**
- 서비스 워커 오프라인 캐시. 등산 중 타일이 안 보이는 문제를 해결하려면 필요하지만,
  타일 캐시 용량 정책까지 얽혀서 별도 작업이다.
- 주소 → 좌표 자동 변환(지오코딩). 외부 서비스 의존이 생긴다. 현재는 주소를 보여주고
  지도를 해당 구간(3~5km 축척)으로 옮겨주는 것으로 대신한다.
- 14개 코스(1,2,3,36~46)의 시작·종료 지점명. 원본에 정보가 없어 비워뒀다.
  `data/courses.meta.csv` 에 채우면 재빌드 시 반영된다.

**미해결로 남긴 것**
- 14개 코스(1,2,3,36~46)의 시작·종료 지점명이 원본에 없다. `data/courses.meta.csv`에
  채우면 재빌드 시 반영된다. 추측으로 채우지 않았다.
- `nam-27` 고도 이상 (평균절대오차 38.4m) 원인 미확정
- 모바일 세로 화면에서 트레일이 작게 보인다. 가로로 긴 경로를 전부 담아야 하니
  fitBounds로는 불가피하다 — 랜딩 목적에는 맞다.

**좌표 대신 주소가 들어 있는 사진 (갤럭시 등)**
- 갤럭시 등 일부 기기는 좌표 대신 **주소 문자열**을 XMP / IPTC 에 넣는다.
  `exifr` **lite 빌드에는 IPTC 태그 사전이 아예 없어**(City=0 확인) 그런 사진은
  "위치 정보 없음"으로만 보였다. **full 빌드로 교체**했다 — 사진 등록 시에만
  지연 로딩되는 청크라 14.6 → 26.5KB gzip 차이는 감수할 만하다.
- **exifr 은 IPTC 의 charset 태그(1:90 CodedCharacterSet)를 무시하고 항상 Latin1 로 읽는다.**
  실측: `1:90 = ESC%G`(UTF-8)를 넣어도 `학동고개` 가 `íëê³ ê°` 로 나온다.
  문자를 바이트로 되돌려 UTF-8 로 재디코딩해 복구한다(`fatal: true` + 한글 포함
  확인으로 진짜 Latin1 텍스트는 건드리지 않는다).
- 주소를 위치 지정 배너·폼·보기 모달에 노출하고, **주소를 우리 코스 데이터와
  매칭해 지도를 그 구간으로 먼저 이동**시킨다. 외부 지오코딩 서비스를 쓰지 않는다 —
  이미 17개 시·군과 90개 코스의 지점명을 갖고 있다.
  실측: `학동고개 거제시` → 22~23코스(축척 3km), `미륵산 정상 통영시` → 통영시 6개(5km).
  전국 축척(100km)에서 클릭하는 것과 비교하면 정밀도가 크게 올라간다.
- 폼에 메타데이터 진단(어떤 키가 있었는지 + 복사 버튼)을 넣었다. 기기마다
  넣는 위치가 달라서, 안 되는 사진이 나오면 이걸로 어디를 봐야 하는지 알 수 있다.

**단계 7 구현 메모**
- **데이터 캐시 버스터.** `index.html` 과 `data/*.json` 은 파일명에 해시가 없어
  GitHub Pages 캐시(`max-age=600`)에 걸린다. 새 JS 가 옛 `courses.json`(예: `eleLow` 없음)을
  받으면 화면이 깨진다. `__BUILD_ID__` 를 쿼리로 붙여 깬다.
  값은 **`meta.json` 의 `generatedAt`** 에서 뽑는다. 처음엔 `Date.now()` 로 만들었는데
  그러면 소스가 같아도 빌드마다 산출물이 달라져 (a) 빌드가 재현되지 않고
  (b) 내용이 그대로인데도 매 배포마다 사용자가 JS 를 다시 받는다. 실제로 배포본
  해시가 로컬 빌드와 어긋나는 것을 보고 되돌렸다. 데이터가 바뀔 때만 바뀌어야 맞다.
- **CSP 를 meta 태그로 걸었다.** 토큰을 브라우저에 보관하므로 실행 코드 출처를 조인다.
  서드파티 스크립트가 0개라 `script-src 'self'` 가 가능하다. 빌드 산출물에 인라인
  스크립트가 없음을 확인했고, 타일 3종·GitHub API·raw 사진이 모두 통과하는 것을 실측했다.
  `style-src` 에 `'unsafe-inline'` 은 남겼다 — Leaflet 이 style 속성을 직접 넣는다.
- **ZIP 백업.** `fflate` 로 `notes.json` + `photos/` + `thumbs/`. JPEG 는 이미 압축돼
  있어 `level: 0` 으로 담는다. 가져오기는 동기화와 같은 LWW 병합이라 **멱등**하다
  (같은 백업 두 번 넣어도 2개 유지, 전부 skipped 로 실측 확인).
- 없는 스크립트(`build:notes`)를 `package.json` 에서 제거했다. 단계 6b에서 단일 파일
  방식으로 바꿔 불필요해진 것을 지우지 않고 있었다.
- **Lighthouse 는 돌리지 않았다.** 대신 수동 점검: `lang=ko`, title/description,
  `color-scheme`, `theme-color`(라이트/다크), CSP, 아이콘 버튼 `aria-label`,
  `img alt`, 입력 레이블, 탭 타겟 24px 이상 - 모두 통과.

**기록이 많아질 때 (2026-08-27)**

사진 60장을 넣고 실제로 확인했다. 세 가지를 고쳤다.

- **목록**: 개수 제한 없이 전부 렌더하고 있었다. 기록마다 DOM 노드와 blob URL 이
  생기므로 수백 장이면 구간을 바꿀 때마다 그만큼 만들고 해제한다.
  **20개씩 + "더 보기"** 로 바꿨다.
- **지도 핀**: 전부 그리고 있었다. **화면 범위 안 + 최대 150개**만 그리고
  지도를 움직이면 다시 계산한다. 실측: 60장 중 17코스 뷰에서 5개, 53코스 뷰에서 1개.
- **상한이 조용히 누락되지 않게** 상태줄에 "지도에 N개 표시 (확대하면 더 보입니다)"를
  띄운다. 안 그러면 "사진이 사라졌다"고 오해한다.

**Leaflet 줌 애니메이션이 완료되지 않는 문제 (2026-08-27)**

핀 작업 검증 중 발견했다. `전체보기`·`전체 구간`을 눌러도 지도가 움직이지 않았다.

진단 로그로 확인한 것: 유효한 bounds 를 만들고 목표 줌(8)을 정확히 계산하고
`map.fitBounds` 까지 호출되는데 줌이 12에서 그대로였다. 같은 호출에
**`animate: false` 를 주면 즉시 8로 이동했다.** 핀을 모두 치워도 재현되므로
핀 개수와 무관하다.

근본 원인은 확정하지 못했다. `fitBounds` 의 기본값을 **`animate: false`** 로 바꿨다 —
이 앱에서 줌 애니메이션은 부가 기능이고, 구간을 바꿀 때 즉시 이동하는 편이 오히려
반응이 빠르게 느껴진다. 호출부가 필요하면 `{ animate: true }` 로 덮어쓸 수 있다.

> 진단 과정에서 `map.off('moveend')` 로 Leaflet 내부 핸들러까지 떼어내 지도를
> 망가뜨린 적이 있다. 그 이후 측정값은 신뢰할 수 없어 새로고침 후 다시 측정했다.

**사진 리포 설정에서 막히는 두 지점 (2026-08-27)**

리포와 토큰만 만들면 되는 게 아니다. 실제로 걸리는 것 둘:

1. **갓 만든 빈 리포에는 기본 브랜치가 없다.** 커밋이 하나도 없으면 `main` 브랜치가
   존재하지 않고, 우리 `putFile` 은 항상 `branch` 를 지정해 보낸다. 생성 시
   "Add a README file" 을 체크해 초기화해야 한다.
2. **사진 리포가 비공개면 사진이 다른 기기에서 안 보인다.** 사진은
   `raw.githubusercontent.com` 으로 읽는데, 비공개 리포의 raw URL 은 인증이 필요하고
   `<img>` 태그는 Authorization 헤더를 보낼 수 없다. **공개로 만들어야 한다** —
   즉 URL 을 아는 누구나 사진을 볼 수 있다는 뜻이다.

**"연결 확인"이 메모 리포만 검사하고 있었다.** 사진 리포는 보지 않아서, 확인을
통과해도 동기화가 실패할 수 있었다. 두 리포를 모두 검사하고 항목별로
통과/경고/실패를 보여주도록 고쳤다 — 쓰기 권한, 빈 리포, 기본 브랜치 불일치,
비공개 여부까지 각각 진단한다. 네 가지 시나리오로 검증했다.

**사진 리포는 여러 트레일이 공유한다 (2026-08-27 결정)**

해파랑길·서해랑길을 같은 구조로 만들 계획을 고려해 사진 리포를 **범용**으로 설계했다.

- 사진 파일명이 **노트 UUID** 라 트레일이 섞여도 충돌하지 않는다 (기술적으로 이미 가능했다)
- 다만 경로가 `{연도}/{id}.jpg` 라 리포를 열어봐도 어느 트레일 사진인지 알 수 없었다.
  **`{폴더}/{연도}/{id}.jpg`** 로 바꿨다. 설정의 `photoPrefix`(기본 `namparang`)가 폴더가 된다.
  사진이 쌓인 뒤엔 경로 이동이 번거로우므로 **비어 있는 지금 정했다.**
- 기본 리포명을 `KDT-Namparang-photos` → **`trail-photos`** 로 바꿨다.
- 코리아둘레길은 실제로 이어진다 — 남파랑길 종점 땅끝탑이 서해랑길 시점이고,
  시점 오륙도는 해파랑길 종점이다. 접합부 사진은 어느 한 트레일의 것이 아니다.

**함께 고친 취약점**: 노트에 사진의 **절대 URL** 을 저장하고 그것만 읽고 있었다.
사진 리포 이름을 바꾸거나 계정을 옮기면 기존 사진이 전부 깨진다.
경로는 안정적이므로 **현재 설정 + 경로로 URL 을 조립**하고, 경로가 없는 옛 노트만
저장된 URL 로 폴백한다. 리포명 변경 시에도 정상 동작하는 것을 확인했다.

**브라우저 로그인은 가능한가 — 불가 (2026-08-27 실측)**

PAT 붙여넣기를 GitHub 로그인으로 대체할 수 있는지 브라우저에서 직접 확인했다.

| 시도 | 결과 |
|---|---|
| `fetch('https://github.com/login/device/code')` | **차단** — TypeError: Failed to fetch |
| `fetch('https://github.com/login/oauth/access_token')` | **차단** |
| `fetch('https://api.github.com/rate_limit')` | 200 (CORS 허용) |
| `fetch('https://api.github.com/user', {credentials:'include'})` | **차단** |

- GitHub 의 토큰 엔드포인트(`github.com/login/*`)는 CORS 를 허용하지 않는다.
  OPTIONS 프리플라이트에도 `Access-Control-Allow-Origin` 이 없다.
- **이미 github.com 에 로그인돼 있어도 그 세션을 쓸 수 없다.** `api.github.com` 은
  `Access-Control-Allow-Origin: *` 를 주는데, 와일드카드는 `credentials` 와 병용 불가다.

→ 정적 사이트만으로는 불가능하다. 프록시 1개(예: Cloudflare Worker)를 두면 Device Flow 가
가능해지지만, OAuth App 토큰의 `repo` 스코프는 **사용자의 모든 리포**에 쓰기 권한을 준다.
지금의 fine-grained PAT(리포 2개 Contents 한정)보다 권한이 넓어지므로,
localStorage 에 두는 토큰에는 **오히려 후퇴**다. PAT 방식을 유지한다.

대신 설정 부담을 줄였다: 토큰을 **붙여넣는 순간 자동 검증**하고,
설정 후 80일이 지나면 동기화 줄에 만료 경고를 띄운다.

**단계 6b 구현 메모**
- **계획을 하나 바꿨다.** 메모를 노트당 파일 1개(`data/notes/{id}.json`)로 두려 했는데
  단일 파일(`public/data/notes.json`)로 바꿨다. 노트당 파일이면 목록을 얻으려고
  디렉터리 조회(인증 필요·API 한도) + 파일마다 요청이 필요해 450개면 451번이다.
  단일 사용자라 충돌이 드물고, 충돌하면 409 가 오니 다시 읽어 병합·재시도하면 정확하다.
- **쓰기 경로에서 sha 와 내용을 반드시 함께 읽어야 한다.** sha 는 API(즉시 최신),
  내용은 raw(최대 5분 캐시)로 따로 읽으면 방금 다른 기기가 올린 변경을 못 본 채
  최신 sha 로 덮어써서 **되돌린다.** `getFileJson` 이 Contents API 하나로 둘을 준다.
- 동기화가 노트를 쓸 때는 `putNoteRaw`(updatedAt 보존)를 쓴다. `saveNote`(항상 지금으로 갱신)를
  쓰면 내려받은 노트가 매번 "방금 수정됨"이 되어 LWW 병합이 수렴하지 않는다.
- 원격에서 내려온 노트의 썸네일 Blob 은 받지 않는다. 원격 raw URL 을 `<img src>` 에
  그대로 넣어 브라우저 캐시에 맡긴다 (450개를 미리 받으면 9MB다).
- 한글 JSON 은 `btoa` 로 바로 못 넘긴다(Latin1 전용). `TextEncoder` 로 UTF-8 인코딩 후 base64.
- 사진은 `putBlobIfAbsent` — 한 번 올리면 바뀌지 않으므로 이미 있으면 건너뛴다.

**단계 6a 구현 메모**
- `exifr` 기본 진입점은 full 빌드(25.5KB gzip)다. `lite`(14.6KB)에 gps·촬영일시·HEIC 가
  다 있어 그걸 쓴다. `mini`(8.9KB)는 HEIC 가 없어 아이폰 원본을 못 읽는다.
- **`pick` 옵션은 lite 빌드에서 예외를 던진다** ("undefined is not iterable") —
  작은 빌드는 태그 이름 사전이 잘려 있다. 기본 `parse(file)` 가 좌표와 촬영일시를
  한 번에 주므로 두 번 읽던 것을 한 번으로 합쳤다.
- **위치 지정 중 Leaflet 레이어 클릭을 통과시켜야 한다.** Leaflet 은 작은 마커
  (radius ≤ 10)를 클릭하면 `e.latlng` 을 **마커 좌표로 바꿔서** 넘긴다.
  그대로 두면 클릭한 자리가 아니라 코스 시작점에 사진이 박히고 코스 정보 카드까지 열린다.
  pane 에 `pointer-events: none` 만 걸면 안 통한다 — Leaflet 이 path/마커 요소 자체에
  `pointer-events: auto` 를 명시하므로 요소를 직접 겨냥해야 한다.
- 삭제는 **소프트 삭제**(`deleted: true`). 단계 6b에서 기기 간 삭제를 전파하려면
  "삭제됐다"는 사실이 남아야 한다. 용량이 큰 사진 Blob 은 즉시 지운다.
- ~~사진 원본은 보관하지 않는다. 1280px / JPEG 0.75 로 줄여 저장한다~~
  → 1차 UAT에서 원본 크기 유지로 변경. 아래 "1차 UAT 피드백" 참조.
- 메모는 `textContent` 로만 렌더한다. `innerHTML` 을 쓰면 단계 6b의 PAT 가 XSS 표적이 된다.

**UX 수정 (단계 5 이후)**
- 코스 정보 카드가 지도 attribution 과 겹쳐 글자가 뭉개졌다. 카드를 `.mapwrap`
  (지도 + 프로필)에 붙여서 `bottom` 기준이 프로필 밑단이 됐던 탓이다.
  **지도 행만 덮는 `.map-overlay` 층**을 따로 두고 거기에 붙인다.
- 시작 코스를 바꾸면 **종료 코스도 같은 코스로** 맞춘다. 종료가 90에 남아 있으면
  원하는 종료 코스까지 목록을 한참 스크롤해야 했다. 더불어 종료 목록에서
  시작보다 앞선 코스는 선택 불가로 막았다 — 고르면 조용히 시작/종료가 뒤바뀌는데
  그게 더 놀랍다.
- **그리드 자동 배치를 명시 배치와 섞으면 안 된다** (두 번째로 겪었다).
  `.map-overlay` 만 `grid-area: 1/1` 로 잡았다가 자동 배치되던 `.map` 이 2행으로
  밀려 높이 0 이 됐다. `.mapwrap` 의 세 자식 배치를 전부 명시했다.

**사이드바 접기 (단계 5 이후 추가)**
- 넓은 화면: 상단바 `◀`/`▶` 버튼으로 접고 펼친다. 좁은 화면에서는 이 버튼을 숨긴다 —
  시트 손잡이가 이미 접기를 담당하는데 버튼이 둘이면 헷갈린다.
- 접힘 상태는 **`localStorage`** 에 둔다. URL이 아니다. URL은 공유용이고,
  남에게 보낸 링크가 받는 사람의 패널 상태까지 강제하면 안 된다.
  (시크릿 모드 등에서 접근 자체가 막힐 수 있어 읽기/쓰기를 try/catch로 감쌌다)
- 지도 컨테이너 크기가 바뀌는 경로가 셋(사이드바 접기 · 프로필 접기 · 창 크기)이라,
  그때마다 `invalidateSize()`를 부르는 대신 `ResizeObserver` 하나로 통합했다.
  안 부르면 Leaflet이 옛 크기로 타일·좌표를 계산해 지도가 어긋난다.

**단계 5 구현 메모**
- 차트 라이브러리를 넣지 않았다. 필요한 건 면적 + 선 + 커서 하나뿐이고
  uPlot(40KB)·Chart.js(70KB)는 "가벼운 스택" 제약에 정면으로 어긋난다. 인라인 SVG 약 250줄.
- `courses.json`에 **`eleLow`(코스당 32점, +4.9KB gzip)** 를 실었다. 전 구간 프로필을
  상세 파일 92개(745KB) 없이 즉시 그리기 위한 것이다. 1,464km를 600px에 그리면
  픽셀당 2.4km라 32점으로 충분하고, 구간을 좁히면 50m 간격 상세로 교체된다.
- 지도 커서 좌표는 **거리(m)가 아니라 비율(0~1)** 로 폴리라인을 따라 보간한다.
  지도 선은 단순화된 것이라 실제 코스 길이와 조금 달라서, 비율로 맞추면 그 차이가 상쇄된다.
- SVG 높이를 상수(132px)로 박았더니 모바일 CSS(104px)와 어긋나 컨테이너를 28px 넘치고
  **문서 전체에 스크롤바가 생겼다.** 컨테이너 높이를 읽도록 고쳤다.
- 좁은 화면에서 하단 시트를 `position: absolute`로 띄우니 고도 프로필을 완전히 덮었다.
  시트를 **그리드 행**으로 바꿨다 — 겹침이 구조적으로 사라지고, 예전에 z-index 1000
  Leaflet 컨트롤이 시트를 덮던 문제도 같이 해결됐다.
- 프로필은 본 코스만 이어 붙인다. 임시노선을 넣으면 경로를 두 번 세게 된다.

**단계 4 구현 메모**
- 상세 라인은 선택 구간이 **30개 이하일 때만** 받는다. 코스당 8KB라 90개면 745KB인데,
  전 구간을 고르는 줌에서는 오버뷰(코스당 43점)와 눈으로 구분되지 않는다.
- 구간 합계는 `distanceM`(원시 미터)로 더한다. 반올림된 `distanceKm`를 90개 더하면 0.5km 어긋난다.
- 난이도 경계는 `meta.json`으로 내려보낸다. 클라이언트에 상수를 중복 정의하면
  `scripts/lib/config.mjs`와 어긋날 수 있다.
- 선택 밖 코스의 투명도를 0.28로 두면 밝은 OSM 타일에서 사실상 사라진다. 0.45로 올려
  전체 경로 안에서 현재 구간의 위치가 보이게 했다.
- Leaflet 컨트롤 컨테이너는 `z-index: 1000`으로 하단 시트 위에 그려진다.
  attribution은 표기 의무가 있어 가리지 않고, 시트에 아래 여백을 줘 내용이 묻히지 않게 했다.

**단계 3에서 추가 검증된 것**
- **총 상승 29,217m vs 총 하강 29,228m — 차이 11m (0.0%).**
  해수면에서 시작해 해수면에서 끝나는 경로라면 물리적으로 같아야 하는 값이다.
  코스별 편차도 22코스 +198m / 23코스 −199m처럼 학동고개를 오르내리는 실제 지형과 맞는다.
  고도 파이프라인에 대한 독립적인 검증이다.

---

## 1차 UAT 피드백 (1차 초도버전 배포 후, 2026-08-28)

실사용 중 발견된 4건. 근본 원인부터 확인하고 고쳤다.

1. **모바일에서 GitHub 동기화 설정 버튼이 안 보임.**
   `main.js`의 사이드바 접기 버튼과 `notes.js`의 동기화 설정(⚙) 버튼이
   둘 다 `.icon-btn` 클래스를 썼는데, 모바일 CSS가 "좁은 화면에서는 시트
   손잡이가 접기를 담당하니 버튼이 둘이면 헷갈린다"는 이유로 `.icon-btn` 을
   통째로 숨겨서 설정 버튼까지 같이 사라졌다. 사이드바 버튼에 `.side-toggle`
   전용 클래스를 주고 그 클래스만 숨기도록 고쳤다.

2. **모바일에서 사진 좌표 추출이 안 됨.**
   진짜 GPS EXIF가 박힌 JPEG을 만들어 Android Chrome UA + 375px 뷰포트로
   `readExif()`/`processImage()`를 직접 호출해 봤는데 좌표를 정확히 뽑아냈다
   (파싱 로직 자체는 뷰포트나 UA와 무관하다 — 순수 JS 바이트 파싱이라 모바일
   에뮬레이션에서 다를 이유가 없다). 즉 **이 앱의 코드 문제가 아니다.**
   iOS Safari와 최신 Android의 시스템 사진 선택기(Photo Picker)는 웹사이트가
   `<input type="file">`로 사진 보관함에 접근할 때 **개인정보 보호 차원에서
   GPS 태그를 제거하고 넘긴다** — 브라우저가 받기 전에 이미 지워진 데이터라
   클라이언트 코드로는 복구할 수 없다. 기존에 있던 "지도 클릭으로 위치 지정"
   경로(`askLocation`)가 정확히 이 상황을 위한 정식 대응이다. EXIF 파싱
   자체는 고칠 게 없었다.

   **추가 후속 조치 (같은 날, 사용성 재검토 후):** 트레킹 중 "방금 찍고 바로
   등록"하는 흐름이라면 사진의 EXIF가 아니라 **그 순간의 실시간 GPS**
   (`navigator.geolocation`)를 쓰면 된다 — OS가 사진에서 GPS를 지워도
   브라우저 자체 위치 API는 영향받지 않는다. `askLocation` 배너에 "📍 지금
   여기(현재 위치) 사용" 버튼을 추가했다. 촬영 시각이 15분 이내면 버튼을
   살짝 강조한다(`is-suggested`). 지도 클릭과 경쟁시키고(`Promise` 레이스,
   먼저 들어온 값이 이김) 먼저 완료된 쪽이 다른 쪽을 취소한다.
   **완전 대체는 아니다** — 나중에(그날 저녁 등) 몰아서 올리는 사진은 이
   방법이 틀린 위치를 주므로, 지도 클릭 경로를 그대로 남겨 뒀다.
   권한 거부·타임아웃은 배너를 유지한 채 토스트로 안내하고 지도 클릭으로
   넘어갈 수 있게 처리했다. 목업 지오로케이션으로 성공/거부/취소/지도클릭
   네 경로를 모두 확인했다.

   **추가 후속 조치(같은 날, UX 재검토):** 지도를 정확히 짚기 어려운 상황을
   위해 좌표·주소를 직접 입력하는 칸을 배너에 추가했다. 외부 지오코딩
   서비스는 쓰지 않는다 — 좌표(`35.0997, 129.1236` 형식)는 그대로 파싱해서
   바로 확정하고, 범위를 벗어나면(위도 ±90·경도 ±180) 주소로 넘긴다. 주소는
   기존에 있던 `matchAddress()`를 그대로 재사용해 우리 90개 코스의
   지점명·지역과 대조하고, 일치하면 지도를 그 근방으로 옮긴다(예:
   "거제시 학동고개" → 22~24코스로 이동) — 정확한 지점은 여전히 지도
   클릭으로 짚는다. 일치하는 게 없으면 토스트로 알리고 배너는 그대로
   유지해 다시 시도할 수 있게 했다. 좌표 성공 · 주소 매치 · 주소
   불일치 · 범위 밖 값(주소 경로로 폴백) 네 경우를 실제 업로드
   시뮬레이션으로 확인했다.

3. **등록된 사진을 리포 주인만 볼 수 있음.**
   `notes.js`의 `load()`는 로컬 IndexedDB만 읽었다. 토큰이 없으면 원격을
   전혀 안 보니, 다른 방문자는 주인이 동기화한 사진을 영원히 볼 수 없었다.
   그런데 **쓰기(사진 업로드·notes.json 커밋)만 토큰이 필요하지, 공개 리포
   읽기는 누구나 할 수 있다** — `github.js`의 `readJson`이 이미
   raw.githubusercontent.com 을 인증 없이 쓴다. `sync.js`에 `pullPublic()`을
   추가해 페이지 로딩 시 토큰 없이 조용히 호출하고, 결과를 로컬에 병합한다.
   빌드/CI 파이프라인을 새로 두는 대신 이 방법을 택한 이유는 훨씬 가볍고
   (배포 자동화 불필요), 주인이 동기화를 누른 즉시(다음 리로드부터) 방문자
   화면에도 반영되기 때문이다. 실제 운영 리포(`yeonkyupark/KDT-Namparang`)를
   대상으로 토큰 없이 호출해 검증했다 — 소프트 삭제된 3건은 제외하고
   살아 있는 1건("코리아둘레길 시작지점")만 정확히 내려왔다.

4. **사진이 리사이즈돼 원본 화질로 안 보임.**
   사용자가 등록 전에 이미 사진을 정리·리사이즈해 두므로, 앱이 저장용
   사본을 1280px/JPEG 0.75로 다시 압축하는 건 화질만 깎는 불필요한 단계였다.
   `processImage()`가 이제 `full`로 원본 File을 그대로 돌려준다 —
   재인코딩도, 리사이즈도 없다. 목록·핀용 썸네일(256px)만 그대로 생성한다.

5. **지도 사진 UI/UX — 표시 on/off, 밀집 시 그룹화.**
   상단 툴바에 "사진" 토글 버튼을 추가했다(고도/전체보기와 같은 자리) —
   누르면 지도 위 핀만 꺼지고 목록은 그대로 남는다.
   밀집 문제는 클러스터링으로 풀었다. 화면 픽셀 거리(42px) 안에 있는 핀을
   그리디하게 묶어 개수가 적힌 원(`.note-cluster`)으로 표시하고, 클릭하면
   그 클러스터의 bounds로 확대한다 — 확대하면 화면 픽셀 거리가 벌어져
   자연히 개별 핀으로 갈라진다. `leaflet.markercluster` 같은 라이브러리를
   추가하지 않고 직접 구현했다(기존 Douglas-Peucker·CSV 파서처럼 이 프로젝트가
   가벼운 스택을 위해 취해 온 방식과 일치). 기존 150개 하드 캡은
   `MAX_CANDIDATES=600`(클러스터링 전 후보 상한, 안전장치일 뿐)으로 대체했다 —
   이제 캡에 걸릴 일은 실질적으로 없다.
